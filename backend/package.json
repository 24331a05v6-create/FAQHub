{
  "name": "faq-platform-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node utils/seed.js",
    "seed:admin": "node -e \"const m=require('mongoose'),U=require('./models/User');require('dotenv').config({path:'.env'});(async()=>{await m.connect(process.env.MONGODB_URI);const u=await U.findOne({email:process.env.EMAIL_USER});if(u){u.role='admin';await u.save();console.log('Promoted to admin:',u.email)}else{await U.create({name:'Admin',email:process.env.EMAIL_USER,password:'Sai@1919',role:'admin'});console.log('Admin created')}await m.disconnect()})()\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.4",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.3",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
